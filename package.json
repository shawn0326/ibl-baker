{
  "name": "ibl-baker-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test:rust && npm run test:js && npm run test:e2e-loader",
    "test:rust": "cargo test --workspace",
    "test:js": "npm run test -w @ibltools/loader",
    "test:e2e-loader": "npm run test -w @ibltools/e2e-loader",
    "check:rs": "cargo check --workspace",
    "check:ts": "tsc -b packages/loader packages/e2e-loader --noEmit",
    "dev:e2e-loader": "npm run dev -w @ibltools/e2e-loader",
    "fixtures:refresh": "node scripts/refresh-fixtures.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^6.0.2"
  }
}
