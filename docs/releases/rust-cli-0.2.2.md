# Rust/CLI 0.2.2

Date: 2026-09-18

Add `ibl-baker --version` and `ibl-baker -V`, reporting the Rust CLI package version. The version flags work consistently through the standalone executable and the npm launcher.

The Rust crates retain their coordinated versioning. Baking behavior, IBLA v1, and KTX2 output contracts are unchanged.
